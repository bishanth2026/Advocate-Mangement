/* Quick actions and activity timeline inside Case 360. */
(function(){
  'use strict';
  var activeCase=null,lastSignature='',refreshTimer=null;
  function read(){try{return JSON.parse(localStorage.getItem('advocateDeskData')||'{}')||{};}catch(e){return {};}}
  function esc(v){return String(v==null?'':v).replace(/[&<>\"']/g,function(m){return {'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#39;'}[m];});}
  function findCase(){var s=read(),heading=document.querySelector('#content h1');if(!heading||heading.textContent.trim()!=='Case 360')return null;var text=document.querySelector('#content .page-title p');var number=text?text.textContent.replace(/.*—\s*/,'').trim():'';return (s.cases||[]).find(function(c){return String(c.number||'').trim()===number||String(c.id||'').trim()===number;})||null;}
  function action(label,fn){var b=document.createElement('button');b.type='button';b.className='secondary';b.textContent=label;b.addEventListener('click',function(e){e.preventDefault();e.stopPropagation();fn();});return b;}
  function visibleModal(){var nodes=document.querySelectorAll('#modal,[role="dialog"],.modal,.dialog');for(var i=nodes.length-1;i>=0;i--){var n=nodes[i];if(n&&n.offsetParent!==null)return n;}return document.getElementById('modal')||document.body;}
  function fieldMatches(el,words){var text=((el.name||'')+' '+(el.id||'')+' '+(el.placeholder||'')+' '+(el.getAttribute('aria-label')||'')).toLowerCase();if(el.parentElement)text+=' '+(el.parentElement.innerText||'').toLowerCase();return words.some(function(w){return text.indexOf(w)>=0;});}
  function setField(el,value){if(!el||value==null||value==='')return false;var v=String(value),done=false;if(el.tagName==='SELECT'){var hit=Array.prototype.slice.call(el.options||[]).find(function(o){return String(o.value)===v||String(o.text).trim()===v||String(o.text).toLowerCase().indexOf(v.toLowerCase())>=0;});if(hit){el.value=hit.value;done=true;}}else{el.value=v;done=true;}if(done){el.dispatchEvent(new Event('input',{bubbles:true}));el.dispatchEvent(new Event('change',{bubbles:true}));}return done;}
  function prefill(){if(!window.ADCase360ActiveCase)return;var c=window.ADCase360ActiveCase,m=visibleModal(),els=Array.prototype.slice.call(m.querySelectorAll('input,select,textarea')),caseValue=c.id||c.number||'',clientValue=c.clientId||c.client_id||'';els.forEach(function(el){if(fieldMatches(el,['case_id','caseid','case number','case no','case name','select case','related case']))setField(el,caseValue);else if(fieldMatches(el,['client_id','clientid','client name','select client','related client']))setField(el,clientValue);});m.querySelectorAll('input[type="hidden"]').forEach(function(el){var n=(el.name||el.id||'').toLowerCase();if(n.indexOf('case')>=0)setField(el,caseValue);if(n.indexOf('client')>=0)setField(el,clientValue);});}
  function open(type){window.ADCase360PrefillType=type;if(typeof window.openModal==='function')window.openModal(type);else if(typeof window.navigate==='function')window.navigate(type==='hearing'?'hearings':type==='task'?'tasks':type==='document'?'documents':'client-management');[50,180,450,900].forEach(function(ms){setTimeout(prefill,ms);});}
  function signature(s){var defs=['hearings','meetings','tasks','documents','discussions','fees','payments'];return defs.map(function(k){return JSON.stringify(s[k]||[]);}).join('|');}
  function timeline(force){var content=document.getElementById('content');if(!content||!activeCase)return;var old=content.querySelector('[data-case360-timeline]');if(old&&!force)return;if(old)old.remove();var s=read(),id=String(activeCase.id||''),number=String(activeCase.number||'');var defs=[['hearings','Hearing','date','title'],['meetings','Meeting','date','title'],['tasks','Task','dueDate','title'],['documents','Document','date','name'],['discussions','Discussion','date','subject'],['fees','Case Fee','date','description'],['payments','Payment','date','description']];var items=[];defs.forEach(function(d){(s[d[0]]||[]).forEach(function(r){var rid=String(r.caseId||r.case_id||'');if(rid!==id&&rid!==number)return;items.push({type:d[1],date:r[d[2]]||r.created_at||'',label:r[d[3]]||r.subject||r.description||r.name||d[1],detail:r.notes||r.details||r.status||''});});});items.sort(function(a,b){return String(b.date).localeCompare(String(a.date));});var box=document.createElement('section');box.setAttribute('data-case360-timeline','1');box.style.cssText='margin:18px 0;padding:16px;border:1px solid var(--border,#e5e7eb);border-radius:12px;background:var(--panel,#fff);';var h=document.createElement('h2');h.textContent='Case Activity Timeline';h.style.margin='0 0 12px';box.appendChild(h);if(!items.length){var empty=document.createElement('p');empty.textContent='No related activity recorded yet.';empty.style.opacity='.7';box.appendChild(empty);}else{var list=document.createElement('div');list.style.cssText='display:grid;gap:10px;';items.forEach(function(it){var row=document.createElement('div');row.style.cssText='padding:10px 12px;border-left:3px solid var(--accent,#64748b);background:var(--surface,#f8fafc);border-radius:6px;';row.innerHTML='<strong>'+esc(it.type)+'</strong> — '+esc(it.label)+(it.date?' <small>('+esc(it.date)+')</small>':'')+(it.detail?'<div style="opacity:.75;margin-top:3px">'+esc(it.detail)+'</div>':'');list.appendChild(row);});box.appendChild(list);}content.appendChild(box);lastSignature=signature(s);}
  function add(){var content=document.getElementById('content');if(!content)return;var heading=content.querySelector('h1');if(!heading||heading.textContent.trim()!=='Case 360'){activeCase=null;return;}activeCase=findCase();if(!activeCase)return;window.ADCase360ActiveCase=activeCase;var title=content.querySelector('.page-title');if(title&&!content.querySelector('[data-case360-actions]')){var bar=document.createElement('div');bar.setAttribute('data-case360-actions','1');bar.style.cssText='display:flex;gap:8px;flex-wrap:wrap;margin:0 0 16px;padding:12px;background:var(--panel,#fff);border:1px solid var(--border,#e5e7eb);border-radius:12px;';bar.appendChild(action('＋ Hearing',function(){open('hearing');}));bar.appendChild(action('＋ Meeting',function(){open('meeting');}));bar.appendChild(action('＋ Task',function(){open('task');}));bar.appendChild(action('＋ Document',function(){open('document');}));bar.appendChild(action('＋ Payment',function(){if(typeof window.p1TransactionModal==='function')window.p1TransactionModal('payment',activeCase.id);else open('finance');}));bar.appendChild(action('＋ Case Fee',function(){if(typeof window.p1FeeSetup==='function')window.p1FeeSetup(activeCase.id);else open('finance');}));title.insertAdjacentElement('afterend',bar);}timeline(false);}
  function refresh(){if(!activeCase)return;var content=document.getElementById('content');if(!content||!content.querySelector('#content h1'))return;var s=read(),sig=signature(s);if(sig!==lastSignature)timeline(true);}
  function boot(){var root=document.getElementById('content');if(!root)return;new MutationObserver(add).observe(root,{childList:true,subtree:true});window.addEventListener('advocate:case-data-changed',function(){timeline(true);});window.addEventListener('storage',function(){timeline(true);});refreshTimer=setInterval(refresh,1500);add();}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();